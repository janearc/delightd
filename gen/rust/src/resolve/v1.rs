// Code generated by protoc-gen-rust-serde. DO NOT EDIT.
// source: resolve/v1/resolve.proto

use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
#[serde(default)]
pub struct ResolvedService {
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "scheme")]
    pub scheme: String,
    #[serde(rename = "address")]
    pub address: String,
}

#[cfg(test)]
mod protojson_tests {
    use super::*;

    #[test]
    fn resolved_service_protojson() {
        let d = ResolvedService::default();
        // proto3 omits defaults: every field absent on the wire decodes to the default.
        assert_eq!(serde_json::from_str::<ResolvedService>("{}").unwrap(), d);
        let jd = serde_json::to_value(&d).unwrap();
        // carries its lowerCamel protojson key.
        assert!(jd.get("name").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("scheme").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("address").is_some());
    }

}
