apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fleet

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: fleet

resources:
  - zookeeper.yaml

# Relocated from kafka-svc (repo kafka-logging, kube/zookeeper.yaml) so delightd
# owns the manifest ahead of that repo's sunset (T8). Manifest kept verbatim.
