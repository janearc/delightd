apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fleet

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: fleet

resources:
  - schema-registry.yaml

# Relocated from kafka-svc (repo kafka-logging, kube/schema-registry.yaml) so
# delightd owns the manifest ahead of that repo's sunset (T8). Kept verbatim.
